"use client";

import { PlaceholderCard } from "@/components/ui/Card";

/**
 * Portfolio vs VNINDEX equity-curve comparison — placeholder until the
 * net-worth time series is wired in.
 */
export function PortfolioVsVnindexPlaceholder() {
  return <PlaceholderCard title="Portfolio vs VNINDEX" module="portfolio" />;
}
